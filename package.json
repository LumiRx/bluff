{
  "name": "bluff",
  "version": "3.0.0",
  "description": "BLUFF — poker structure, word deduction",
  "private": true,
  "scripts": {
    "icons": "python3 tools/icons.py",
    "build": "node tools/build.js",
    "serve": "npx --yes http-server dist -p 8080 -c-1",
    "cap:add": "npx cap add ios && npx cap add android",
    "cap:sync": "npm run build && npx cap sync",
    "ios": "npm run cap:sync && npx cap open ios",
    "android": "npm run cap:sync && npx cap open android",
    "test": "node test.js && node flow.js && node audio.js && node fit.js && node store.js && node home.js && node items.js && node muck.js && node clock.js && node chest.js"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/haptics": "^6.0.2",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/push-notifications": "^6.0.2",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2",
    "playwright": "^1.62.1"
  }
}
